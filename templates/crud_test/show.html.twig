{% extends 'base.html.twig' %}

{% block title %}Students{% endblock %}

{% block body %}
    <h1>Students</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ student.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ student.name }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ student.age }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_crud_test_index') }}">back to list</a>

    <a href="{{ path('app_crud_test_edit', {'id': student.id}) }}">edit</a>

    {{ include('crud_test/_delete_form.html.twig') }}
{% endblock %}
